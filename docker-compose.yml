services:
  web:
    image: nginx:alpine
    container_name: xtremeplugins-web
    ports:
      - "2070:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
